// Empty hook to resolve build error
// This file is not being used but is referenced somewhere in the build process
export const useScrollDirection = () => {
  return {
    scrollDirection: 'none',
    isScrolling: false
  };
};
